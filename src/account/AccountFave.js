import React from 'react'

export default function AccountFave({fave}) {

    console.log(typeof(fave));

    return (
        <div>
            This will display the routes that have been favourited. 
        </div>
    )
}